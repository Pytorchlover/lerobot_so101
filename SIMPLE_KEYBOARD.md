# 简化的键盘控制（Wayland 兼容）

## 新的按键映射

现在支持**单字符输入，不需要按 Enter**！

### 方式 1: 使用箭头键（推荐）
- **右箭头键 (→)**: 下一个 episode（跳过当前）
- **左箭头键 (←)**: 重新录制当前 episode
- **ESC键**: 退出采集

### 方式 2: 使用字母键（如果箭头键不工作）
- **n**: 下一个 episode（跳过当前）
- **r**: 重新录制当前 episode
- **q**: 退出采集

## 使用说明

1. 运行数据采集程序
2. 程序会自动检测 Wayland 并启用终端输入模式
3. **直接按单个键即可**，不需要按 Enter
4. 按键会在下一个循环中生效

## 提示

- 确保终端窗口有焦点（点击终端窗口）
- 按键是实时响应的，不需要等待
- 如果箭头键不工作，使用字母键（n/r/q）

