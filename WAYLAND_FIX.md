# Wayland 键盘监听问题解决方案

## 问题
在 Wayland 下，`pynput` 无法监听全局键盘事件，导致键盘快捷键无法工作。

## 解决方案

### 方案 1: 切换到 X11（推荐，最简单）

#### 步骤 1: 检查当前会话类型
```bash
echo $XDG_SESSION_TYPE
# 如果显示 wayland，需要切换
```

#### 步骤 2: 切换到 X11

**方法 A: 通过登录界面切换（推荐）**
1. 注销当前会话
2. 在登录界面，点击用户名下方的齿轮图标 ⚙️
3. 选择 "Ubuntu on Xorg" 或 "Xorg"
4. 输入密码登录

**方法 B: 修改配置文件（如果登录界面没有选项）**
```bash
# 编辑 GDM 配置
sudo nano /etc/gdm3/custom.conf
```

找到这一行并取消注释：
```ini
#WaylandEnable=false
```
改为：
```ini
WaylandEnable=false
```

保存后重启系统。

#### 步骤 3: 验证
登录后检查：
```bash
echo $XDG_SESSION_TYPE
# 应该显示 x11
```

然后重新运行数据采集程序，键盘应该可以正常工作了。

---

### 方案 2: 使用终端输入替代方案（Wayland 兼容）

如果不想切换显示服务器，可以使用终端输入读取作为替代。我已经创建了一个兼容 Wayland 的版本。

#### 使用方式
在数据采集时，在终端中输入：
- `n` + Enter: 下一个 episode（相当于右箭头键）
- `r` + Enter: 重新录制当前 episode（相当于左箭头键）
- `q` + Enter: 退出采集（相当于 ESC 键）

#### 启用方法
运行数据采集程序时，添加环境变量：
```bash
LEROBOT_USE_TERMINAL_INPUT=1 lerobot-record [你的参数]
```

---

## 推荐方案

**强烈推荐方案 1（切换到 X11）**，因为：
1. 最简单，一次设置永久解决
2. 不需要修改代码
3. 所有键盘快捷键都能正常工作
4. X11 在 Ubuntu 22.04 上完全支持

方案 2 适合不想切换显示服务器的用户，但需要修改使用习惯。

