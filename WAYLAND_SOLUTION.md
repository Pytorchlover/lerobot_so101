# Wayland 键盘问题解决方案

## 问题确认
你的系统使用 Wayland (`XDG_SESSION_TYPE=wayland`)，而 `pynput` 在 Wayland 下无法监听全局键盘事件。

## 解决方案

### 方案 1: 切换到 X11（推荐，最简单）

**步骤：**
1. 注销当前会话
2. 在登录界面，点击用户名下方的齿轮图标 ⚙️
3. 选择 **"Ubuntu on Xorg"** 或 **"Xorg"**
4. 输入密码登录

**验证：**
```bash
echo $XDG_SESSION_TYPE
# 应该显示 x11
```

然后重新运行数据采集程序，键盘快捷键（右箭头、左箭头、ESC）应该可以正常工作。

---

### 方案 2: 使用终端输入模式（已实现，Wayland 兼容）

我已经修改了代码，现在**自动检测 Wayland 并使用终端输入模式**。

**使用方式：**
运行数据采集程序时，程序会自动检测到 Wayland 并切换到终端输入模式。你会看到：

```
============================================================
Terminal Input Mode (Wayland compatible)
============================================================
Commands:
  n + Enter: Next episode (skip current)
  r + Enter: Rerecord current episode
  q + Enter: Quit recording
============================================================
```

**操作：**
- 在终端中输入 `n` 然后按 Enter → 下一个 episode（相当于右箭头键）
- 在终端中输入 `r` 然后按 Enter → 重新录制当前 episode（相当于左箭头键）
- 在终端中输入 `q` 然后按 Enter → 退出采集（相当于 ESC 键）

**注意：**
- 需要在运行数据采集的终端窗口中输入
- 输入后需要按 Enter 键
- 命令会在下一个循环中生效

---

## 推荐

**强烈推荐方案 1（切换到 X11）**，因为：
1. ✅ 一次设置，永久解决
2. ✅ 所有键盘快捷键都能正常工作
3. ✅ 不需要改变使用习惯
4. ✅ X11 在 Ubuntu 22.04 上完全支持且稳定

方案 2 适合不想切换显示服务器的用户，但需要适应新的输入方式。

---

## 测试

修改后的代码已经自动支持 Wayland。直接运行你的数据采集命令：

```bash
lerobot-record [你的参数]
```

如果检测到 Wayland，会自动使用终端输入模式。你会看到提示信息。

